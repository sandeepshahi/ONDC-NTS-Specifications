summary: RSF_NIL
details:
 - description: detailed flow of RSF (NIL FLOW)
   mermaid: >-
     sequenceDiagram
      participant NP(Collector/Receiver)
      participant Settlement_Agency
      NP(Collector/Receiver) ->> Settlement_Agency: settle API 
      Settlement_Agency ->> NP(Collector/Receiver) : on_setttle API
     
references: if any
steps:
  - summary: SETTLE
    api: settle
    details:
      - description: settle api
        mermaid: >-
         sequenceDiagram
         participant NP(Collector/Receiver)
           participant Settlement_Agency
           NP(Collector/Receiver) ->> Settlement_Agency: settle API      
           Settlement_Agency ->> NP(Collector/Receiver) : on_setttle API
    reference: if any
    example:
      value:
        $ref: "../../examples/RSF/settle/settle_null.yaml"
  - summary: ON_SETTLE
    api: on_settle
    details:
      - description: on_settle
        mermaid: >-
         sequenceDiagram
         participant NP
          participant Settlement_Agency
          NP ->> Settlement_Agency: settle API 
          Settlement_Agency ->> NP : on_setttle API
    reference: if any
    example:
      value:
        $ref: "../../examples/RSF/on_settle/on_settle_null.yaml"